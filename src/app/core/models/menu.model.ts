export interface MenuItem {
  id: string;
  title: string;
  gradient: string;
}

export interface PromoItem {
  title: string;
  description: string;
  gradient: string;
}

export interface Stats {
  bottles: number;
  points: number;
  co2: number;
}
