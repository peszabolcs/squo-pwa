{
  "framework": "angular",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/squo-pwa/browser",
  "installCommand": "npm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
